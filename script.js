window.addEventListener("DOMContentLoaded", function(event) {
    console.log("The DOM has been loaded");
    console.log("Not only that, but the script should be uglified now, attempt to read it...");
});